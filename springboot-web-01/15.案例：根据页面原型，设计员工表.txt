步骤：
    1.阅读并分析页面原型及需求
    2.分析表中包含哪些字段，以及字段的类型、约束
    2.创建表结构

注意：
    1.在数据库中一般并不储存图片本身，而是储存图片的访问路径。
    2.手机号这种固定的数字也用varchar。
    3.后端一般只管varchar设置最大值，最小值一般由前端设置
    4. 创建表时额外的基础字段：
        -- id主键;
        -- create_time创建时间（通过now()这个函数获取当前时间）;
        -- update_time修改时间;